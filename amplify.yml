version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build process required for static site"
    build:
      commands:
        - echo "Static site deployment"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
